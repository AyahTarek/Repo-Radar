export const DEFAULT_CHART_HEIGHT = 320;

export const CHART_MARGIN = { top: 16, right: 16, bottom: 72, left: 56 } as const;

/** Longer repo names are truncated on the axis; the tooltip keeps the full name. */
export const MAX_AXIS_LABEL_LENGTH = 14;
